export type expense = {
  title: string;
  amount: number;
  date: Date;
  completed: boolean;
  id: string;
}

export type finance = {
  title: string;
  amount: number;
  date: Date;
  received: boolean;
  id: string;
}